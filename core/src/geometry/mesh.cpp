/*
 * Eine reine Datenstruktur. Sie hält zwei Listen (Vektoren): vertices (Punkte
 * im Raum) und indices (welche Punkte bilden ein Dreieck). Das ist das
 * "Skelett" deiner Fläche.
 */

/*
#include <glm>

struct Vertex {
    glm::vec3 position;  // x, y, z
    glm::vec3 normal;    // Wichtig für Licht/Schattierung
};

class Mesh {
public:
    std::vector<Vertex> vertices;   // Alle Punkte
    std::vector<uint32_t> indices;  // Die "Verbindungsliste"

    // Methode, um die Daten an die GPU zu senden (VAO, VBO, EBO)
    void setupMesh();
};*/
